# Log

## 2026-07-03 — Full-app audit + fixes (bugs, dead code, data, decision conflicts)

- **Audit method:** attempted an 11-agent parallel review workflow first; every agent died on the org's monthly spend limit, so the whole audit ran inline instead — scripted checks (tsc, a JSON integrity script over all 9 data files, unused-export scanner) plus a systematic read of the data layer, all 6 stores, and the major screens. Backed up the repo to the session scratchpad before any edit, since the project has no version control.
- **Verified claims against reality, and progress.md lost twice:** (1) the Thailand gap-fill session claimed per-city meta was recomputed — Bangkok/Chiang Mai/Ayutthaya/Nakhon Pathom meta counts were all still stale and Phetchabun was missing entirely; (2) the ICT-timezone session's rationale for keeping `todayKey()` device-local ("drives the daily-chaos-draw reset") referenced a subsystem that was already 100% dead — no screen ever called drawDaily. Both are why doc-vs-code conflict checks were part of the audit dimensions.
- **Confirmed meta is runtime-dead before deciding how to fix it:** grepped every `.meta` usage — nothing reads the JSON meta blocks at runtime. Chose to recompute them anyway (files self-describe; future integrity checks become signal), not because the app needs it.
- **Biggest real bug was a silent category misbucket:** 57 Thailand quests (Park/Scenic/Landmark/Festival) fell through toCategory's default to 'Social' — wrong filters, tints, and user-quest multipliers. Found by diffing every distinct raw category string in the data against CATEGORY_MAP's keys, not by reading screens. The card-stamp map already handled all four, which is what made the gap provable rather than a taste call.
- **Removed two whole dead subsystems, not just scraps:** the old PRD badge model (zone/city/region/country checkers + types + storage keys) that contradicted the live medal/jigsaw model, and the daily-chaos-draw system. Both removals verified by grep-before-delete (zero call sites) and tsc-after.
- **Deliberately did NOT touch:** KH/VN food-tier cutoffs (content balance — the gap-fills skewed Cambodia to 56 Common vs the contract's 9; flagged with exact numbers instead), TRIP_START on reset (arguably tracks the real trip, not the save), offbeat_seasia_destinations.json (unused but possibly future content — fixed its misleading comment only), and the blank `* copy.md` template files.
- **Storage-key hygiene without a migration:** the chaos store's ad-hoc `KEYS.COMPLETED_QUESTS + '_chaos'` became a registered `KEYS.COMPLETED_CHAOS` with the identical string value, so existing saves keep working with zero migration code.
- **Verification:** tsc clean; integrity script 0 issues (was 8); removed-symbol grep clean; live preview — onboarding → SEA board → Today → chaos draw through the refactored store, "2×" badge intact, food avail count (464) exactly matches the per-country sums. No console errors.

## 2026-07-03 — Cambodia quest audit + 57-quest gap-fill: 2 new cities

- **Checked the schema before reusing the Laos/Vietnam script wholesale** — Cambodia's JSON has no `quest_type` field and a different id scheme (`KH_<CITY>_<NUM>`, no region segment) and only 8 raw categories (no Heritage/Slow Travel/Viewpoint). Adjusted the add-quest script to match rather than writing fields the schema doesn't use.
- **The audit surfaced a genuine structural gap that neither the activities nor food audit called out directly as a "missing city"** — Sihanoukville & Koh Kong has its own full food-audit region (distinct beach/seafood identity: Ochheuteal/Otres night markets, Koh Kong smoked fish) but zero city presence in the app; only the Koh Kong mangrove interior existed, filed under Cardamom Mountains. Found this by cross-referencing food-audit regions against the city list directly, the same technique that caught Vietnam's Soc Trang gap.
- **Distinguished genuine dish gaps from wording variants carefully, since Cambodia's data already covers an unusually high fraction of the food audit** (Cobra Bile Shot, Scorpion Rice Wine, Gecko Rice Wine, Fertilized Duck Egg, Puffed Fried Whole Frog were all already present under different section headers). Verified each candidate gap with direct keyword grep against full quest text before counting it, rather than trusting a single fuzzy-match pass — the first automated pass over-reported by more than 2x before manual verification.
- **Left the "extreme" tail of the food audit (#108-129) explicitly unresolved** rather than guessing — the audit itself flags these as "not individually detailed in source," so per the user's own "flag anything ambiguous rather than guessing" instruction, reported them as a block rather than pretending to have verified each one with the same confidence as the fully-detailed dishes.

## 2026-07-03 — Vietnam gap-fill: 6 new cities + 44 new quests

- **Checked whether new cities needed code changes before assuming they didn't** — `vietnam.ts` derives `VIETNAM_REGIONS`/`VIETNAM_CITIES` live from the quest JSON's `cities[]` array, so adding a city is purely a data change. Verified this by grepping for any hardcoded Vietnam city list elsewhere (world.ts's `DETAILED_CITIES`, MapScreen's hall logic) before committing to a data-only approach — found none, confirmed via `tsc` and a live zone-map check on the new Soc Trang city.
- **Didn't silently drop the two ethically-loaded dishes** (Thit Cho — dog meat; Thit Rung — wild game, legally sensitive per the audit's own caveat). The user's instruction was to turn every gap into a quest with no scope carve-out, so both got quests — written factually, stating the controversy/legal caveat in the description itself rather than either omitting them or writing them as uncomplicated recommendations.
- **Kept Yen Tu Mountain in the existing Ha Long city rather than making it a 7th new city** — it's a different site but the same province (Quang Ninh), and Ha Long already represents that province in the data. New cities were reserved for provinces with zero existing representation (Soc Trang, Tra Vinh, Hau Giang, Bac Ninh, Thai Binh, Hung Yen).

## 2026-07-03 — Laos quest audit + 40-quest gap-fill

- **Audit first, content second, per two separate user turns** — the audit itself was explicitly scoped as read-only ("don't draft new quest content yet — show me the gap list first"), so I did the full cross-reference and stopped there. The user reviewed the gap report in a follow-up message and gave a clear go-ahead ("turn all of those into new quests... just make new quests for those") — only then did I write any data.
- **Cross-checked the audit source material for staleness before trusting it:** found extra files in the user's `travel app/laos/` folder (`laos_quests_complete_updated.json`) that looked like they might be a newer, more complete source than what's in the app. Diffed it against the app's committed JSON — same 361 quests, same exact gaps. This ruled out "just re-import a newer file" as a shortcut and confirmed the gaps were genuine content gaps from a later audit pass, not a sync problem.
- **Found and correctly diagnosed a misplacement, not just a missing item:** "Kong Phanang Panorama & Tham Pulan Caves" existed in the game data already — just filed under Thakhek instead of Savannakhet. Caught by comparing its coordinates (16.30, 104.90) against both cities' actual distance (~30km from Savannakhet vs ~130km from Thakhek's loop), not just by title-matching. Relocated rather than treating it as a fresh add, which would have created a duplicate.
- **Wrote a one-off script instead of 40 manual edits** for the actual data insertion — at this volume, manual JSON edits risk id collisions and zone typos. Explicitly re-verified every new quest's `zone` value exists in its city's declared `zones[]` array before considering the batch done, because that exact bug (a quest zone missing from the city's zone list, making the quest invisible on the map) was found and fixed for Bangkok in an earlier session — did not want to reintroduce the same class of bug at 40x the volume.
- **Made judgment calls on genuinely ambiguous city placement without re-asking**, since the user had already delegated that ("just make new quests for those"): Vat Boun Huang → Luang Namtha not Muang Sing (matches its river-route description); Wat Muang Kang → Champasak not Pakse (matches its province description). Documented the reasoning in progress.md rather than silently picking one.
- **Declined to fabricate false GPS precision** for the audit's vaguer entries (e.g. "western part of town," "near bus station") — used the city's base coordinates as an honest, visibly-approximate anchor instead of inventing a precise-looking but made-up lat/lng, while using real approximate knowledge for well-documented UNESCO-zone Luang Prabang temples where that knowledge is actually reliable.

## 2026-07-02 — Food-dish tier rework (Common/Rare/Unique) + Odyssey dice roll

- **Stopped and re-confirmed twice before writing any code** — the user's own instructions explicitly said the tier migration mapping was "a content call, not a coding one" to confirm first. First answer was garbled/cut-off and referenced a "legendary" bucket that contradicted the original 3-tier spec (Common/Rare/Unique, with Unique explicitly replacing epic+legendary). Rather than guess which was right, asked a direct clarifying question — confirmed 3 tiers stands, no 4th tier. Two of three rounds of AskUserQuestion surfaced real, material ambiguities the user hadn't fully specified (Thailand has no literal Must-Try/Nice-to-Try labels, only rarity+flags; Cambodia/Vietnam have no dish pool at all).
- **Caught a softlock before it shipped:** naively remapping Cambodia/Vietnam's Food quests by their existing location-quest rarity (mythic/legendary/epic/rare) would have given both countries exactly 0 Unique-tier dishes — since neither has a single legendary/mythic Food quest. That would make the dice roll's Unique target permanently unmeetable. Caught this by actually computing the distribution before proposing a mapping, not after. Fixed by using each country's own XP natural-break points instead of the shared rarityFromXP thresholds (which were calibrated for all quest types, not food specifically).
- **Kept `food_tier` deliberately separate from `rarity`** rather than overloading the existing field for Cambodia/Vietnam — those countries' Food quests are still real location quests whose `rarity`/`xp` drive QuestCard styling, region XP, and leveling elsewhere in the app. Overwriting `rarity` for them (the way it's safely done for Thailand/Laos's true dish-pool quests, which never render through QuestCard) would have quietly broken those other systems. This is the kind of modeling mistake that would've been invisible until someone completed a Cambodian temple quest and its card looked wrong.
- **Verified the persistence claim, not just the roll:** did a full page reload mid-test and confirmed via both the UI and a raw localStorage dump that the exact same dice values survived — not just "it didn't visibly re-roll," but the actual persisted JSON matched before and after.

## 2026-07-02 — Legendary Quests halls restyled as collectible cards

- **User confirmed the sparser Legendary Hall counts (from the rarity rework) are a feature, not a problem** — they like it. Noted as a validated call, not something to walk back.
- **Reused the Food Hall's exact pattern rather than inventing a new card language:** `QuestCollectibleCard` is a near-verbatim structural copy of `FoodCard` (band/emoji/footer layout, same width math as `FoodHallView`'s 2-column grid) so the two hall experiences read as the same game system. Only the palette source differs — rarity-driven (`RARITY_CARD_PALETTE`) instead of xp-threshold-driven (`foodPalette`), since these aren't food items.
- **Emoji comes from the new `card_category` field**, not the old 5-bucket `category` — reused the existing category-badge work instead of inventing a second categorization.
- **Deliberately dropped the inline "add to inventory" button** that the old `QuestCard`-based hall had (via `AddSlot`) when switching to the card grid — matches how the Food Hall already behaves (tap → detail → add there), so behavior stays consistent across both hall types rather than mixing patterns.
- **Verified the palette differentiation, not just presence:** live-checked both a legendary card (deep purple) and Laos's one mythic card (Long Tieng, 25,000 XP) side by side in the same grid — confirmed the near-white "foil" mythic treatment is visually distinct from legendary purple, not just a color swap that reads the same at a glance.

## 2026-07-02 — Rarity rework (xp-based) + 8-category badge stamp for location-based quests

- **Scoped precisely to `baseNormalise()` in `quests.ts`** (regular + legacy + chaos quests) — the two food-dish pools (`loadFoodQuests`, `loadLaosFoodQuests`) bypass that function entirely and were deliberately left on their own tier system, per the user's explicit exclusion.
- **Found and flagged a real consequence before it surprised anyone:** MapScreen's per-country "Legendary Quests" hall filters `rarity === 'legendary'`. Recomputing rarity from xp shrinks that set hard (Cambodia 43→4, Vietnam 39→4, Thailand 32→18, Laos 29→8) since 'legendary' now sits below the new 'mythic' tier. Widened the hall filter to `legendary || mythic` so it stays meaningful, and called this out explicitly rather than letting the halls go quiet without explanation.
- **Category badge is a NEW, separate taxonomy, not a repaint of the existing one:** the existing `category` field (5 buckets: Adventure/Culture/Spiritual/Food/Social) already collapses too much detail to recover Temple vs Market vs Museum vs Heritage vs Nature — so added a second field (`card_category`, 8 buckets) derived straight from the raw category string, used only by the badge stamp. Left the original 5-bucket field and everything keyed to it (filters, zone tints, XP multipliers) untouched.
- **Recognized "the current 12 categories" the user referenced** by checking actual raw category values in the data files — Laos and Vietnam's regular datasets each have exactly 12 distinct raw strings, and the user's named fold-ins (Spiritual→Temple, Trek+Viewpoint→Nature, Slow Travel→Culture) map them exactly onto the 8 target buckets. Extended the same map to cover Thailand/legacy's wider raw vocabulary (Historical/Landmark→Heritage, Wellness→Temple, Sport/Nightlife→Adventure, etc.) — flagged these judgment calls in contract.md rather than deciding silently.
- **Verified with live data, not just types:** node script over all 6 quest JSON files confirmed the new tier counts (mythic=1, legendary=44, epic=945, rare=892) and sampled one quest per tier; tsc clean; live preview (via the shared `expo-web-laos` dev server) confirmed exact accent colors by computed style — epic `#FFD700`, legendary `#4B2E7A`, food `#b18ce0` — and category stamps rendering correctly (MARKET, CULTURE, FOOD, NATURE, ADVENTURE) bottom-right on real cards.

## 2026-07-02 — Four cross-page fixes (map sizing, chaos browse, region-visited, ICT timezone)

- **User asked me to confirm two assumptions before calling this done** (per their own explicit instruction) — both confirmed true via code inspection before writing anything: (1) chaos quests are a real, pre-existing entity (`useChaosStore`, 122 quests) — did not invent a chaos system; (2) `quest.region` is the macro-region field, not country — verified no separate `country` field exists on `Quest`, and country is always a derived lookup.
- **Lucky find:** `QuestLogScreen` already had full `inventoryType:'chaos'` support built (filters, grouping, status) with zero UI entry point wired to it from Today. Item 2 was a 12-line addition, not new feature work — checked for this before assuming I'd need to build a browse screen.
- **Item 3's root cause was an inconsistency already latent in the code, not a missing feature:** Countries/Cities in the same stat tile were ALREADY correctly computed as "visited" (≥1 completed quest, via archive entries) — only Regions used a different, stricter "badge fully unlocked" measure. Recognized this by comparing the three computations side by side rather than assuming a visited-flag needed to be built from scratch. Proved the fix mattered (not coincidental) by reading live persisted state: 2 quests done in Central Thailand (badge `unlocked:false`) — old logic → 0 regions, new logic → 1. Deliberately left `regionBadges.unlocked` (the jigsaw completion achievement) untouched — visited and complete are different, both legitimate concepts now correctly separated.
- **Item 4 scoped narrowly on purpose:** fixed `shortDate`/`tripDay`/`clockTime` (the 3 functions actually feeding Today's date/day-counter and Travel-Dex's clock) to read Indochina Time via `Intl.DateTimeFormat({timeZone:'Asia/Bangkok'})`. Did NOT touch `todayKey()`, which drives the unrelated daily-chaos-draw reset — changing that would've been an unrequested, unrelated behavior change (that reset boundary staying device-local was out of scope). `tripDay` now diffs ICT *calendar dates* (reusing the existing `dayDiff` primitive) rather than raw milliseconds-since-start, so it rolls at ICT midnight as asked, not 24h after an arbitrary start timestamp.
- **Flagged rather than silently expanded:** there's no live clock on Today itself (only a date) — `clockTime()` is only consumed by Travel-Dex's `LiveClock`. Fixed it anyway (it's the app's only real clock) but told the user explicitly rather than assuming that's what "the Today page clock" meant.
- **Verification:** tsc clean; standalone Node test of the ICT logic forcing `TZ=America/New_York` (9/9 checks, including a boundary moment that's still "yesterday" for a US Eastern viewer but already the next ICT day); live preview confirmed all 4 fixes — uniform 2x2 country grid, chaos quests browsable with real data, regions-visited stat proven correct against live persisted state, ICT-formatted date on Today.

## 2026-07-02 — Add Vietnam as 4th country

- **Followed the Cambodia template** (which is itself the Laos template): data-file twin + `<country>.ts` region module + 3-file wiring (countries/quests/world) + hall wiring (FoodHallView/food.ts/MapScreen/TodayScreen). 4th application of the pattern — no new architecture.
- **Caught & fixed a region-key collision before it shipped:** Vietnam's raw "South" region collides with Laos's "South" (countryForRegionKey resolves the first matching country, so Vietnam's South would've been attributed to Laos — breaking badges/food/legendary scoping). Renamed Vietnam's directional keys to globally-unique forms (Northern Vietnam / North-Central Coast / Southern Vietnam), keeping name===key so the key never surfaces oddly in the Today scope label / region-badge text. Verified uniqueness against all 3 existing countries in the smoke test.
- **Normalized the schema inconsistency the MD flagged:** 4 quests (Con Dao ×3 + one Phong Nha) shipped `coords` instead of `coordinates`; collapsed to `coordinates` in the transform (the loader already tolerates both, but normalizing keeps the file a clean twin). Also backfilled Con Dao's missing `base_coordinates` from its quest centroid (same trick used for Cambodia).
- **Verified the MD's "Heritage/Slow Travel unique to Vietnam" concern was already handled** — both were in CATEGORY_MAP (Cambodia/Thailand data already used Heritage). No change.
- **Generalized two prior Cambodia-specific hacks into country-agnostic logic** rather than adding a 4th hardcoded branch: FoodHallView's `isCambodia` → `usesRegularData`; TodayScreen's `cambodiaFood` → `regularFood` (any Food quest whose country has no dish pool). Both now auto-pick-up future dish-pool-less countries.
- **Layout:** Vietnam is a tall tile down the right edge (matches its N–S shape). The right band was too narrow for the "VIETNAM" label (wrapped mid-word), so nudged Laos + Cambodia layout boxes left to widen Vietnam to w24 — purely cosmetic coordinate changes, verified no overlaps. Region board uses a 2-2-1 grid + halls row, red palette.
- **Verification:** tsc clean; 17/17 node smoke checks; full in-app drill-down (board → region → city → zone → quest), Food Hall (78) + Legendary hall (39), Today food chip. No console errors.

## 2026-07-02 — Cambodia Legendary/Food Hall: green outline + position swap

- `CAMBODIA_LEGENDARY_TILE.outline` changed from `COLORS.ORANGE` to `COLORS.GREEN` (MapScreen.tsx) — now bit-for-bit matches Thailand's Cross-Region tile and Laos's Legendary tile (`accent: PURPLE, outline: GREEN`), regardless of Cambodia's own orange country theme. No other legendary tile needed changes — Thailand and Laos were already green.
- Swapped `layout` between `CAMBODIA_FOOD_HALL_TILE` (now x:57, right) and `CAMBODIA_LEGENDARY_TILE` (now x:13, left) — literal position swap only, sizes/other fields untouched.
- Verified live: Cambodia's bottom row now shows Legendary Quests (green outline) on the left, Food Hall (purple outline) on the right; Laos's Legendary tile screenshot-compared side by side to confirm identical green.

## 2026-07-02 — Cambodia food on Today + skip mandatory MY WORLD picker

- **Investigated before editing:** Today's "FOOD QUESTS" section only ever read from the `food_quests` store (Thailand's 147-dish Food Hall pool + Laos's 104-dish pool). Cambodia has no equivalent dedicated dish-pool data file — its food content lives as `category: 'Food'` quests directly inside `cambodia_regular_quests.json`'s city blocks (same source the Map's Cambodia Food Hall already reads from, built in an earlier session). That's *why* Cambodia was invisible here — not a bug, a genuine gap in what feeds this specific list.
- **Decision:** merged Cambodia's Food-category quests into the Today page's local `allFood` array (from `all_quests`) rather than converting them into `food_quests`-pool-shaped objects or duplicating the data into a new file. Reasoning: `food_quests` is consumed elsewhere (FoodHallView.tsx) with different filtering assumptions (Laos dishes have `region: undefined`); safer to leave that pool untouched and do a local, additive merge scoped to where it's actually needed.
- **Root-caused a schema mismatch while merging:** Cambodia's food quests don't carry `food_country`/`food_region` tags (only real dish-pool items do), so the existing `foodCountryOf`/`foodRegionOf` helpers would've silently mis-bucketed every Cambodia quest under `'thailand'`/`'Other'`. Fixed by falling back to the quest's real `region` field (via `countryForRegionKey`) — the same technique already used for the Cambodia Food Hall on the Map screen, so this is consistent with existing precedent, not a new pattern.
- **"Availability" number fix:** re-read the user's phrasing carefully — "mirror the flow of the regular quests above it being country, regional, or level specific" maps directly onto the Food section's own existing country → region → tier chip hierarchy (already built, just not wired to the header badge). The collapsed header was showing a flat `${completedCount}/${totalAllCountries}` that never changed regardless of filter selection — a real, demonstrable bug relative to what the user asked for, not a matter of taste. Fixed by pointing the badge at `availableFood.length` (already computed, already respects the current filter), same value already driving the "X dishes available" line further down. Verified the number now moves: 310 → 59 → 7 as the filter narrows.
- **Map skip logic:** implemented as a derived boolean (`SKIP_CONTINENT_PICKER = exactly one available continent AND it's Southeast Asia`) rather than literally deleting the 'myworld' mode or hardcoding `mode` to always start at 'world' — per explicit instruction, this needs to self-disable the moment a second continent gets real content. Confirmed the back button ("< MY WORLD") and full globe rendering are untouched and reachable.
- **Verification:** tsc clean both changes; live preview confirmed Cambodia dishes browse correctly with real titles/regions, the avail badge scopes with the filter, Map lands directly on Southeast Asia on both fresh launch and tab re-tap, and the globe is still fully functional when manually opened.

## 2026-07-02 — Thailand quest audit → 9 confirmed gap quests + Bangkok zone bug

- **Method:** built a fuzzy-match script (jaccard token overlap + substring containment, city-scoped) cross-referencing the user's 706-item Thailand attractions audit against every quest in `thailand_regular_quests.json`/`thailand_legacy_quests.json`. Manually triaged every low-score "no match" to separate real gaps from phrasing false-negatives (e.g. "The Grand Palace: Temple of the Emerald Buddha" scored low but is obviously the same place as an already-matched item).
- **Found mid-audit, stopped and flagged before proceeding:** while checking Bangkok's zone assignments for the new quests, discovered `constants.ts`'s hand-authored Bangkok zones (5) had drifted from the data file's 10-zone model, making 20/32 existing Bangkok quests — including Grand Palace — invisible on the City Map (exact-string zone match in `getQuestsByZone`). This was bigger than the original ask; paused and got explicit sign-off before fixing, per the project's risk-management norm for anything touching more than the requested scope.
- **Decision, user-confirmed:** cooking class and elephant sanctuary quests written generic/criteria-based rather than naming a specific real business — matches the app's occasional practice of naming real venues (e.g. "Wisarut Muay Thai") only when there's no ethical-reputation risk in endorsing one specific operator by name.
- **Decision, user-confirmed:** Siam Amazing Park (Bang Kapi, east Bangkok) didn't fit any of Bangkok's existing zones — added a new "East Bangkok" zone rather than forcing a bad geographic fit or dropping the quest.
- **New finding surfaced during contract drafting, not in the original gap report:** the existing "IconSiam" quest description already name-checks the floating-market recreation that is Sook Siam. Flagged to user before drafting; user chose to still give it a standalone quest.
- **Verification:** tsc clean; node script confirmed 746 total quests / 0 duplicate IDs / 0 invisible-zone quests; live preview confirmed Grand Palace reachable from Bangkok's City Map and Siam Amazing Park reachable from the new East Bangkok zone.

## 2026-07-02 — Add Cambodia as 3rd country

- **Decision:** Made Cambodia a schema twin of Laos rather than inventing a new structure. Rationale: user said "follow the exact same pattern as Thailand and Laos"; Laos is the established 2nd-country template. Adding a country is a data + 3-file edit (countries.ts / quests.ts / world.ts) + one new `<country>.ts` region module.
- **Decision:** Renamed the delivered `coords` field to `coordinates` in the data file (to match the Laos twin) AND hardened `baseNormalise` to accept `raw.coordinates ?? raw.coords` as insurance for future imports. Belt-and-suspenders; low risk.
- **Decision:** Computed `base_coordinates` per city as the centroid of its quests' coordinates (Laos cities carry base_coordinates; Cambodia's didn't). Derived, not invented — improves region "More" map placement.
- **Data contradiction surfaced (not silently shipped):** "Kampong Cham & Mekong Corridor" appeared in delivered `meta.total_quests_by_city` but had no city entry / zero quests in the array. `meta.total_quests` (212) already excluded it. Removed the phantom meta key and flagged to user rather than shipping an empty city.
- **Count mismatch:** cambodia.md says 210 quests; the `_updated` JSON has 212 (2 extra epic, 2 extra temple). Shipped the JSON as authoritative; noted MD is stale.
- **Scope calls (flagged):** no dedicated Food Hall (no dish pool supplied) and no separate Legendary hall tile for Cambodia — both are optional polish, not required for a country to function. Food + legendary quests still appear within cities.
- **Verification:** tsc clean; 13/13 smoke checks; in-app drill-down (country→region→city→zone→quest) confirmed with live data and no console/bundler errors.

## 2026-07-02 — Cambodia theme/halls + cross-country food zones (follow-up)

- **Orange theme:** country accent + 9-shade orange region palette (literals, since the palette only has one ORANGE/GOLD).
- **Removed Nationwide region.** Decision on where its 10 quests go: user said "put whatever is in there in the corresponding regions." Interpreted as: food → Food Hall view, legendary festivals → Legendary hall view, but each still needs a real home city for map/badge attribution. Rehomed 7 food dishes → Phnom Penh/Food Circuit; 2 nationwide festivals → Phnom Penh/Arts & Culture; Angkor Sankranta → Siem Reap/Angkor Core Temples (it's literally at Angkor).
- **Food Hall mechanism (asked user):** chose "all Cambodia food quests" as a cross-cutting VIEW (not a dish-pool collectible hall like TH/Laos), since Cambodia has no dish pool. Implemented by generalizing RegionDetailView's bucket branch to support kind='food' (filter category==='Food') alongside kind='legendary'.
- **Bugfix while here:** the legendary-hall scoping was a Laos/non-Laos binary; adding Cambodia would have made Thailand's Cross-Region hall wrongly include Cambodia legendary quests. Refactored to explicit per-country HALL_SPEC.
- **Cross-country food zones (asked user):** chose "re-tag existing food quests, cities-with-food only." Rule: reuse a zone whose name contains "Food"; else append "Food & Markets"; then set every category==='Food' quest's zone to it. Used contains-"Food" (not a loose regex) to avoid false positives like Laos "Night Sky & Nature". Only Chiang Mai is both food-having AND hand-authored in constants.ts → paired edit there.
- **Discovered pre-existing meta drift** (not mine): Laos meta 369 vs array 361; Thailand meta 736 vs array 737. Flagged, left as-is.
- **Verification:** tsc clean; orphanZones=0 & foodOutsideFoodZone=0; in-app confirmed Cambodia halls (59 food / 43 legendary), orange theme, and Chiang Mai's new FOOD & MARKETS zone tile.

## 2026-07-02 — Cambodia board polish (follow-up 2)

- **Combined South Coast + South Coast Islands:** moved "Koh Rong & Islands" city from region "South Coast Islands" into "South Coast"; removed the South Coast Islands region def. Cambodia now has 8 geographic regions.
- **Board layout:** rows 1-2 stay 3-column; row 3 (Southwest + South Coast) and row 4 (Food Hall + Legendary Quests) are 2-column, centered and vertically aligned (x=13/57, w=30). Rows now fill the RegionMap container top-to-bottom (y 3→95, matching Thailand's ~96) so the dead space above the "MY PLACES" section is gone (this was the "extra space at the bottom" complaint — RegionMap uses a fixed aspectRatio 0.62, so filling vertically is the fix, not resizing the shared container).
- **Food Hall cards:** the Cambodia Food Hall bucket now renders FoodCard (2-col collectible cards) instead of QuestCard, matching the Thailand/Laos Food Halls. Added BUCKET_FOOD_CARD_W mirroring FoodHallView's width calc.
- **Minor known cosmetic:** Cambodia food quests have no `food_category`, so FoodCard shows the generic 🍜 fallback emoji (Thailand/Laos dishes carry food_category → varied emoji). Structurally identical; flagged for later if varied emoji is wanted.
- **Verification:** tsc clean; in-app confirmed combined region, centered/aligned bottom two rows, no bottom dead space, and FoodCard rendering (RARE/EPIC/LEGENDARY tier bands + XP + emoji).

## 2026-07-02 — Varied food emojis (follow-up 3)

- baseNormalise now sets `food_category` for any category==='Food' quest via guessFoodCategory(title+description) — the same keyword-guess Laos dishes use. FoodCard then shows a fitting emoji + the category as its subtitle instead of the generic 🍜/"FOOD".
- Applies to all regular food quests, but only visibly affects the Cambodia Food Hall (Thailand/Laos food halls use their own dish pools; their regular food quests render via QuestCard in zone sheets, which ignores food_category).
- Result: 13 distinct emoji categories across Cambodia's 59 food quests. Classification is keyword-based/approximate (Lao-tuned regex on Khmer names) — a few dishes land in a close-but-imperfect bucket (e.g. Fried Tarantulas → Grilled rather than Insects). Easy to fine-tune by extending guessFoodCategory with Khmer keywords if desired.
- Verified in-app: varied emojis (🦞🦗🦐🥐🥗🥢🍜🍲🍛🍚🍢🍤🍮) render in the Cambodia Food Hall.

## 2026-07-02 — Cambodia Food Hall uses full FoodHallView (follow-up 4)

- Switched the Cambodia Food Hall from the plain RegionDetailView bucket list to the real FoodHallView (banner + region chips + rarity chips + region-grouped sections), matching Thailand/Laos.
- Mechanism: Cambodia's Food Hall tile now uses the shared FOOD_HALL_KEY (→ 'foodHall' mode) instead of a bucket key. FoodHallView gained a `country: 'cambodia'` branch that sources Food-category quests from `all_quests` (Cambodia has no dish pool) and groups/filters by geographic `region` instead of `food_region`. Added CAMBODIA_FOOD_REGION_ORDER (food.ts).
- Cleaned up MapScreen: removed CAMBODIA_FOOD_KEY, the food branch from the bucket renderer/HALL_SPEC (now legendary-only via HALL_COUNTRY), and the now-unused FoodCard/Dimensions imports + BUCKET_FOOD_CARD_W.
- Region chips correctly show only regions that have food quests (Remote Northwest is omitted — Banteay Chhmar has none).
- Verified in-app: banner "…in Cambodia… 0/59 collected", ALL REGIONS + region chips, rarity chips, and region-grouped FoodCard sections (e.g. NORTHERN CAMBODIA (7)). tsc clean, no console errors.
